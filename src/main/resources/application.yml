spring:
  datasource:
    url: jdbc:mysql://localhost:3306/store-multimarcas?createDatabaseIfNotExist=true
    username: root
    password: 123456
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: true

jwt.public.key: classpath:keypublic.pem
jwt.private.key: classpath:keyprivate.pem